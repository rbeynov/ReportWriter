package com.ynov.reportwriter;

public interface Printable {
    // No body part for the method because it must be specificly implemented by every class that implements the interface
    // public visibility access modifier is implicit, as well as abstract keyword !
    void print();
}
